# Infrastructure

Infrastructure configuration for local development and future deployment.

## Folders

- `docker/`: local Docker Compose for PostgreSQL, Redis, MinIO, and optional app containers.
- `kubernetes/`: sample production manifests for the API, admin portal, config, secrets, ingress, and dependency notes.
- `nginx/`: reverse proxy configuration notes.
