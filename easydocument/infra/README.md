# Infrastructure

Infrastructure configuration for local development and future deployment.

## Folders

- `docker/`: local Docker Compose for PostgreSQL, Redis, and MinIO.
- `kubernetes/`: future Kubernetes manifests.
- `nginx/`: future reverse proxy configuration.
